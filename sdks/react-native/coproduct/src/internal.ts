// Internal surface for conformance verification. Bucket access is not part of
// the public API and is reachable only through this subpath, mirroring the
// per-platform internal visibility used by the other SDKs.
export { bucketForVectors } from './generated/coproduct_ffi_uniffi';
